#include <stdio.h>
#include "main.h"

/**
 * more_numbers - Entry point
 * Prints
 * @void: nothing
 * Return: Void
 */

void more_numbers(void)
{
int a;
int b;
 
for (b = 0; b <= 9; b++)
{
for (a = 0; a <= 14; a++)
{
if (a < 10)
{
_putchar(a + '0');
}
else
{
_putchar((a / 10) + '0');
_putchar((a % 10) + '0');
}
_putchar('\n');
}
}
}
