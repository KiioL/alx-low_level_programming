#include <stdio.h>
#include "holberton.h"

/**
 * _islower - Entry Point
 * Prints alphabet
 * Return: 1 or 0
 */

int _islower(int c)
{
if (c > 96 && c < 123)
{return (1);}
else if (c > 64 && c < 91)
{return (0);}
}


